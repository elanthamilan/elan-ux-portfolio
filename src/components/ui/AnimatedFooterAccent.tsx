import React from "react";

const AnimatedFooterAccent = () => {
  return (
    <div className="w-16 h-1 bg-gradient-to-r from-brand-primary to-brand-secondary rounded-full opacity-60" />
  );
};

export default AnimatedFooterAccent;
